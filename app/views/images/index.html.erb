<h1>Listing images</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Original</th>
      <th>Final</th>
      <th>Height</th>
      <th>Width</th>
      <th>Project</th>
      <th></th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @images.each do |image| %>
      <tr>
        <td><%= image.name %></td>
        <td><%= image.original %></td>
        <td><%= image.final %></td>
        <td><%= image.height %></td>
        <td><%= image.width %></td>
        <td><%= image.project %></td>
        <td><%= link_to 'Show', project_image_path(image.project,image)%></td>
        <td><%= link_to 'Edit', edit_project_image_path(image.project,image) %></td>
        <td><%= link_to 'Destroy', edit_project_image_path(image.project,image), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Image', new_project_image_path(params[:project_id]) %>
